# ============================================
# PRODUCTION ENVIRONMENT (AWS)
# ============================================
# This file is an EXAMPLE for production deployment
# NEVER commit the actual .env.production file
# Set these as environment variables in ECS Task Definition

# Application
NODE_ENV=production
PORT=3000
API_PREFIX=/api/v1

# Database - AWS RDS (PostgreSQL)
# Get these values from AWS RDS Console after creating your instance
DB_HOST=pandebugger-prod.xxxxx.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=pandebugger_prod
DB_USER=admin
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_FROM_AWS_SECRETS_MANAGER

# Logging
LOG_LEVEL=info

# CORS - Your production frontend URL
CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com

# JWT (Authentication) - MUST be stored in AWS Secrets Manager
JWT_SECRET=USE_AWS_SECRETS_MANAGER_FOR_THIS_VALUE
JWT_EXPIRES_IN=7d

# AWS
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=USE_IAM_ROLE_INSTEAD
AWS_SECRET_ACCESS_KEY=USE_IAM_ROLE_INSTEAD

# Additional Production Settings
# Add these in ECS Task Definition environment variables
# - DB_SSL=true (for RDS SSL connections)
# - DB_MAX_CONNECTIONS=20
# - RATE_LIMIT_WINDOW_MS=900000
# - RATE_LIMIT_MAX_REQUESTS=100
